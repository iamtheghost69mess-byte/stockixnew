# Platform Postgres container — local/VPS-style layout (infra/vps/docker-compose.postgres.yml).
# Replace POSTGRES_PASSWORD before production; mirror the password in apps/api DATABASE_URL.
POSTGRES_USER=postgres
POSTGRES_PASSWORD=stockix_platform_postgres_local_dev
POSTGRES_DB=stockix_platform
