# Platform Postgres — same DATABASE_URL as apps/api/.env (Stockix control plane). Not tenant MySQL.
# Matches infra/dev/docker-compose.yml port 54330.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54330/stockix_platform
