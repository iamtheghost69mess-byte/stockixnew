# Optional root copy for tools that load .env from the monorepo root.
# Prefer packages/db/.env for Drizzle; apps/api/.env for the API.
DATABASE_URL=postgresql://postgres:postgres@127.0.0.1:54330/stockix_platform
