/**
 * Stockix control-plane organization number allocation (ORG-00001).
 * Used during tenant provisioning in the worker; re-exported for API modules.
 */
export { allocateOrganizationNumber } from "@repo/db";
