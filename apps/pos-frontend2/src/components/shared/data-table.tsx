// Re-exported from the shared UI library so this path continues to resolve
// for all existing POS imports while the canonical source lives in @repo/ui-core.
export { DataTable, type DataTableProps } from "@repo/ui-core/data-table";
