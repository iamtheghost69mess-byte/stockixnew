const {
  getOrganizationAccessState,
} = require("./get-organization-access-state");
const {
  controlPlanePermissionMatrix,
} = require("./control-plane-permission-matrix");

module.exports = {
  getOrganizationAccessState,
  controlPlanePermissionMatrix,
};
