import type { paths } from "@restaurant-pos/platform-api";

/** OpenAPI path map generated from `openapi/platform-v1.yaml`. Use for stricter typings alongside Zod. */
export type PlatformV1Paths = paths;
