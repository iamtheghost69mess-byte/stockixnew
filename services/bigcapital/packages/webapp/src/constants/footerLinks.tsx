// @ts-nocheck
/**
 * Setup wizard left column footer links (/setup).
 * Stockix white-label: keep empty so Blog / Community / Support / Docs / app name are hidden.
 *
 * Original upstream links (restored if you want external links — use real URLs):
 *   blog, community, support, docs (intl keys), plus app.app_name home link — all pointed to `#`.
 */
export const getFooterLinks = () => [];
