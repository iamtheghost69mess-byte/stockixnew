# DEPRECATED — pre-shared-infra standalone layout (per-tenant mysql/mongo/redis/nginx/webapp).
# Not used by the Stockix tenant provisioner (infra/tenant-stack + infra/shared).
# Kept for reference only; use infra/tenant-stack/docker-compose.yml for multi-tenant deploys.
#
# This is a production version of the Stockix docker-compose.yml file.

version: '3.3'

services:
  nginx:
    container_name: stockix-nginx-gateway
    build:
      context: ./docker/nginx
      args:
        - SERVER_PROXY_PORT=3000
        - WEB_SSL=false
        - SELF_SIGNED=false
    volumes:
      - ./data/logs/nginx/:/var/log/nginx
      - ./docker/certbot/certs/:/var/certs
    ports:
      - "${PUBLIC_PROXY_PORT:-80}:80"
      - "${PUBLIC_PROXY_SSL_PORT:-443}:443"
    tty: true
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    depends_on:
      - server
      - webapp

  webapp:
    container_name: stockix-webapp
    image: ghcr.io/stockixhq/webapp:latest
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp

  server:
    container_name: stockix-server
    image: ghcr.io/stockixhq/server:latest
    links:
      - mysql
      - mongo
      - redis
    depends_on:
      - mysql
      - mongo
      - redis
    read_only: true
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp
    environment:
      # Mail
      - MAIL_HOST=${MAIL_HOST}
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_SECURE=${MAIL_SECURE}
      - MAIL_FROM_NAME=${MAIL_FROM_NAME}
      - MAIL_FROM_ADDRESS=${MAIL_FROM_ADDRESS}

      # Database
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_CHARSET=${DB_CHARSET}

      # System database
      - SYSTEM_DB_NAME=${SYSTEM_DB_NAME}

      # Tenants databases
      - TENANT_DB_NAME_PERFIX=${TENANT_DB_NAME_PERFIX}

      # Authentication
      - JWT_SECRET=${JWT_SECRET}

      # MONGODB_DATABASE_URL is injected per-tenant via infra/worker-service
      # buildTenantMongoUrl(slug) — do not hardcode shared DB here
      - MONGODB_DATABASE_URL=${MONGODB_DATABASE_URL}

      # Application
      - BASE_URL=${BASE_URL}

      # Agendash
      - AGENDASH_AUTH_USER=${AGENDASH_AUTH_USER}
      - AGENDASH_AUTH_PASSWORD=${AGENDASH_AUTH_PASSWORD}

      # Sign-up restrictions
      - SIGNUP_DISABLED=${SIGNUP_DISABLED}
      - SIGNUP_ALLOWED_DOMAINS=${SIGNUP_ALLOWED_DOMAINS}
      - SIGNUP_ALLOWED_EMAILS=${SIGNUP_ALLOWED_EMAILS}

  database_migration:
    container_name: stockix-database-migration
    build:
      context: ./
      dockerfile: packages/server/Dockerfile
      target: migration
    environment:
      - DB_HOST=mysql
      - DB_USER=${DB_USER}
      - DB_PASSWORD=${DB_PASSWORD}
      - DB_CHARSET=${DB_CHARSET}
      - SYSTEM_DB_NAME=${SYSTEM_DB_NAME}
    depends_on:
      - mysql
    security_opt:
      - no-new-privileges:true
    tmpfs:
      - /tmp

  mysql:
    container_name: stockix-mysql
    build:
      context: ./docker/mariadb
    environment:
      - MYSQL_DATABASE=${SYSTEM_DB_NAME}
      - MYSQL_USER=${DB_USER}
      - MYSQL_PASSWORD=${DB_PASSWORD}
      - MYSQL_ROOT_PASSWORD=${DB_ROOT_PASSWORD}
    volumes:
      - mysql:/var/lib/mysql
    # Stateful database service requires writable data volume.
    security_opt:
      - no-new-privileges:true
    expose:
      - '3306'

  mongo:
    container_name: stockix-mongo 
    build: ./docker/mongo
    expose:
      - '27017'
    volumes:
      - mongo:/var/lib/mongodb
    # Stateful database service requires writable data volume.
    security_opt:
      - no-new-privileges:true

  redis:
    container_name: stockix-redis
    build:
      context: ./docker/redis
    expose:
      - "6379"
    volumes:
      - redis:/data
    # Stateful cache service requires writable data volume.
    security_opt:
      - no-new-privileges:true

# Volumes
volumes:
  mysql:
    name: stockix_prod_mysql
    driver: local

  mongo:
    name: stockix_prod_mongo
    driver: local

  redis:
    name: stockix_prod_redis
    driver: local
